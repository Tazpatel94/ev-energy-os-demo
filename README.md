# EV Energy OS — Depot Simulation Engine (V0.1)

Run:
```bash
pip install -r requirements.txt
python run_demo.py
```

Outputs in `outputs/`:
- baseline_load.png
- optimized_load_{conservative,expected,aggressive}.png
- optimized_schedule_{...}.csv
- summary.json

Data in `data/sessions.csv`.
